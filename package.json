{
  "name": "glsl-auto-ui",
  "version": "0.0.1",
  "description": "Automatic DAT.GUI generation from GLSL uniforms",
  "author": "Szymon Kaliski <contact@treesmovethemost.com>",
  "license": "MIT",
  "main": "./lib/index.js",
  "scripts": {
    "build": "babel src --presets es2015 --out-dir lib",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "glsl-parser": "^2.0.0",
    "glsl-tokenizer": "^2.1.1",
    "lodash.flatten": "^4.2.0",
    "lodash.get": "^4.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-preset-es2015": "^6.6.0"
  }
}
